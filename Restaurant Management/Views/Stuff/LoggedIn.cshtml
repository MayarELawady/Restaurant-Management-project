@model Restaurant_Management.Models.Staff

@{
    ViewBag.Title = "LoggedIn";
}

@if (Session["UserId"] != null)
{
    <h4>Hello @Session["UserName".ToString()]</h4>

    <p>@Html.ActionLink("Update Profile", "UpdateAdminProfile", new { id = Session["UserId"] }, null)</p>
    <p>@Html.ActionLink("List Employees", "ListEmployees")</p>
    <p>@Html.ActionLink("Add New Meal", "AddMeals")</p>


    <!---SEARCHHH-->

    using (Html.BeginForm("SearchEmployee", "Stuff", FormMethod.Post))

    {
        @Html.TextBox("search")
        <input type="submit" value="search" />
    }



}

